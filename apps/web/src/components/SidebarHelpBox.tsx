export function SidebarHelpBox() {
  return (
    <div className="rounded-xl border border-[var(--border)] bg-[var(--callout-blue)] px-3 py-3 text-xs text-[var(--muted)]">
      <p className="font-semibold text-[var(--text)] mb-1">Need help?</p>
      <p className="leading-relaxed">
        Start with Sessions, then Packages, then Messages — that order keeps your account healthy.
      </p>
    </div>
  );
}
